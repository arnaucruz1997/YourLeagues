import { StepperPositionDirective } from './stepper-position.directive';

describe('StepperPositionDirective', () => {
  it('should create an instance', () => {
    const directive = new StepperPositionDirective();
    expect(directive).toBeTruthy();
  });
});
